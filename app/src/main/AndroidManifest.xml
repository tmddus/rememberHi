<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sy.a2018rememberhi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.LoginActivity" />
        <activity android:name=".Activity.CheckActivity" />
        <activity android:name=".Activity.SignupActivity" />
        <activity android:name=".Activity.ProfileActivity" />
        <activity android:name=".Activity.MissionActivity" />
        <activity android:name=".Activity.TodayendActivity" />
        <activity android:name=".Activity.MessageActivity" />
        <activity android:name=".Activity.TodayendList" />
        <activity android:name=".Activity.ViewTodayendActivity" />
        <activity android:name=".Activity.Mission2Activity"></activity>

    </application>

</manifest>